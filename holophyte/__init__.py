"""The Holophyte factory as a package.

`factory.py` is the entry point; the code is moving here one section at a
time (phase 2 of the plan). So far: `target` (where a target's state lives,
the `Target` value) and `config` (`config.toml` and every table it can set).
"""
